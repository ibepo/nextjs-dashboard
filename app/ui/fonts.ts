import { Barlow, Inter, Lusitana } from 'next/font/google';

export const barlow = Barlow({ weight: ['400', '700'], subsets: ['latin'] });
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
